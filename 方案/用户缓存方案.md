# 用户服务缓存优化方案

## 用户服务缓存存在的问题:
1. favors 列表缓存,收藏和订阅是通过类型位图(如000110)生成的组合,且发布时列表未处理组合key(看似自由组合实则写死了).未用到zset,且每页是一个key缓存的整个分页数据
2. 列表分页缓存的是整个res_info数据,占用内存大,无法知道资源变更
3. 历史列表新增和移除数据造成列表数据失效,需要重新缓存
4. 通过page和page_size分页,数据变化会出现漏查或重复(不一致可接受)

## 缓存列表解决方案
### A. 按资源类型缓存数据,服务端合并排序再截取
  - 优缺点: 适合数据量小于5k,主要按时间排序,App通用做法
### B. 按资源类型缓存数据,通过redis做交集
  - 优缺点: 高并发不友好,会生成临时key,占用CPU
### C. 列表全量zset(成员数百万是常见的),服务端做过滤截取或创建子ZSET
  - 优缺点: 简单实用可控
### 分页优化解决方案
> 向后兼容支持cursor.ZSET的ZRANGE有深分页性能问题,高并发更适合score/cursor查询

- 有cursor 参数直接解析
  - 规则: id+score
- ~~没有cursor参数,将page/page_size换算为cursor,并短期缓存.~~
  ```
  Key: page_cursor:{uid}:{scene}:{page}:{page_size}
  TTL: 1~5 分钟
  Value: cursor
  ```
  - scene取值
    - collect_all|collect_:res_type(res_type目前有article|ticker|video|live|dynamic 可按资源自由组合)
    - fans_all|subscribe_all|subscribe_subscription
    - history_all|history_:res_type(res_type目前有article|ticker|video|live|dynamic 可按资源自由组合)

## C方案优化 -> 最终方案
> 选择方案原因: 用户数据天然非热点key且有限,且有组合类型查询需求,非video类型密度容易稀疏.

- 有子类型 ZSET 则直接查询
- 有全量ZSET则用ZSCAN重建子ZSET,再用ZRANGE取数据.
  - 设置子类型列表 TTL 30小时
  - 并维护子类型的反向索引集合
- 没有全量ZSET,需要查询数据库page固定为1查询数据并返回(保证用户体验,必须有返回数据,并防止page过大的深分页问题).作为索引无需TTL
  - 通过其他手段(限长+主动剪裁/清理)处理
  - 必须异步查询所有数据并缓存全量ZSET
  - 需标记防止重复同步
  - 需标记空数据防止重复
  - 成员格式 `${type}_${id}` (ZSCAN前缀匹配性能极佳)
### 安全防御事项
- zscan根据zcard设置batch(返回值非精确)的大小(500<10万<1000),如果子 ZSET 是多类型组合,~~这需要多次扫描或~~执行lua脚本过滤
  - 百万级数据batch 1000次大概 600ms
- 发布资源时,根据反向索引集合判断子ZSET是否存在(要对key做类型过滤).存在则做增量同步,不存在可以删除反向索引集合的key.子zset的构建由下次请求触发
- MySQL查询与并发.
  - 总用户 246048,有效 245245.总profile数据 5878832,history数据 4349541.近一天活跃优化 1435 `SELECT COUNT(DISTINCT account_id) as unique_users FROM profile_infos WHERE type = 'history' AND act_time >= DATE_SUB(NOW(), INTERVAL 1 DAY);`
  - UUID字符串36字节,类型字段4-8字节,Redis额外开销：约8-16字节,总共约 308M 内存
  - (华为云MySQL的QPS平均50,峰值109) 压力场景模拟: 
    - 日活用户10%(24524),高峰3小时假设60%,则大概有14715人. QPS=3*14715/3/3600 ≈ 4.08
    - 所有日活用户同时触发(24525),5分钟内全部访问 收藏/历史/订阅 列表. QPS=3*24525/300 ≈ 246

## favors表与接口

### 日志统计分析
> router_request_uri: #"/history" and pool_usr_name: "k8s_default_fengshows-user-service-svc-nodeport-80_HTTP-80" | select router_request_uri as uri, count(1) as times group by uri limit 20

- 统计最近一天 marker 路由 20 条的次数
  ```
  /user/favors/marker	1,420.00 
  ```
- 统计最近一天 collect 路由 20 条的次数
  ```
  /user/favors/list/collect/article_ticker/3	2.00 
  /user/favors/list/collect/article_ticker/2	3.00 
  /user/favors/list/collect/article_ticker/1	3.00 
  /user/favors/list/collect/article_ticker/0	117.00 
  /user/favors/list/collect/article_ticker/4	4.00 
  /user/favors/list/collect/0	23.00 
  /user/favors/list/collect/video/1	2.00 
  /user/favors/list/collect/video/2	1.00 
  /user/favors/list/collect/video/0	56.00 
  /user/favors/collect	61.00 
  ```
- 统计最近一天 history 路由 20 条的次数
  ```
  /user/history/list/2?res_type=article_ticker	5.00 
  /user/history/list/6?res_type=article_ticker	1.00 
  /user/history/list/2?res_type=video	4.00 
  /user/history/list/0?res_type=video	157.00 
  /user/history/list/0?res_type=live	66.00 
  /user/history/create/_bulk	180.00 
  /user/history/list/5?res_type=article_ticker	1.00 
  /user/history/list/0?res_type=article_ticker	170.00 
  /user/history/create	9,882.00 
  /user/history/list/1?res_type=live	1.00 
  /user/history/del/_bulk	35.00 
  /user/history/list/3?res_type=article_ticker	1.00 
  /user/history/list/4?res_type=article_ticker	1.00 
  /user/history/synchrodata	17,338.00 
  /user/history/list/1?res_type=video	7.00 
  /user/history/list/1?res_type=article_ticker	9.00 
  /user/history/list/3?res_type=video	2.00 
  ```
- 统计最近一天 subscribe 路由 20 条的次数
  ```
  /user/favors/list/subscribe/0	3.00 
  /user/favors/subscribe	113.00 
  ```

### 表与接口 
> 表设计用户后续拆分; 接口结构保持不变,重写接口服务缓存,未使用接口废弃.

- profile/favors
  - 批量计数 `POST /user/favors/detail`
  - `无日志` ~~互动资源 `GET /user/favors/:type/:res_type/resources`~~
  - 收藏 collect
    - 表 user_collect
      ```
      表字段 id/account_id/res_type/res_id/created_time/status
      ```
    - ~~收藏列表 `/user/collect`~~
    - `cursor` 收藏列表 `/user/favors/list/collect/:page`
    - `cursor` 收藏分类列表 `/user/favors/list/:res_type/:page`
    - 添加收藏 `POST /user/favors/collect`
  - 订阅 subscribe
    - 表 user_subscribe
      ```
      表字段 id/account_id/res_type/res_id/created_time/status
      ```
    - 添加订阅 `POST /user/favors/subscribe`
    - `cursor` 粉丝列表 `/user/users/:user_id/subscribe`
    - `cursor` 全部关注列表 `/user/favors/list/subscribe/:page`
    - `cursor` 关注订阅号列表 `/user/favors/list/subscribe/:res_type/:page` ?未使用?
    - ~~所有已关注订阅号 `/user/favors/subscribes/:res_type`~~
  - 历史 history
    - 表 user_history
      ```
      表字段 id/account_id/res_type/res_id/created_time/status/act_ip/act_plt
      ```
    - ~~添加历史 `POST /user/favors/history`~~
    - ~~历史列表 `POST /user/favors/list/history/:page`~~
    - 添加历史 `POST /user/favors/history/create`
    - 批量同步 `POST /user/favors/history/create/_bulk`
    - 批量删除 `POST /user/favors/history/del/_bulk`
    - 同步记录 `POST /user/favors/history/synchrodata`
    - `cursor` 历史列表 `/user/history/list:page`
  - 点赞 marker
    - 表 user_marker
      ```
      表字段 id/account_id/res_type/res_id/created_time/status
      ```
    - 添加点赞 `POST /user/favors/marker`
    - ~~点赞列表 `/user/favors/list/marker/:page`~~
    - ~~点赞资源的用户列表 `/user/favors/marker/:res_type/:res_id/users`~~

- 其他服务调用情况
  - 内容服务
    - 获取用户信息 /user/batch
    - 获取用户身份 /user/detail
    - 审核回答 /user/:user_id/resource
    - /api/v4/users/:id/extra_countries
    - 批量获取点赞详情 /favors/detail
    - 关注訂閲號列表 /favors/subscribes/[].join('_')
    - token验证 /oauth/verify
    - 全部关注 /favors/list/subscribe/:types/:page
    - 关注的话题 /favors/subscribe/topic/resources
    - 用户设置 /user_property
  - 评论服务
  - 消息服务
  - 同步服务
  - 短信服务
  - ump
  - 订阅号
  

## 实施步骤
- 写代码
- 华为控制台测试zscan效率